# Core app
streamlit>=1.40.0
pandas>=2.0.0
numpy>=1.24.0
openpyxl>=3.1.0

# ML
scikit-learn>=1.3.0
xgboost>=2.0.0

# Database
SQLAlchemy>=2.0.0

# Visualization
plotly>=5.18.0

# LLM — the CURRENT Google GenAI SDK (NOT the deprecated google-generativeai)
google-genai>=0.3.0

# PDF report
reportlab>=4.0.0

# Config
python-dotenv>=1.0.0
tabulate>=0.9.0

# --- Optional: Document Chat (RAG) ---
# These are heavy (sentence-transformers pulls in torch, ~2GB). The app runs
# fine without them — the Document Chat feature degrades gracefully and tells
# the user what to install. Uncomment to enable RAG over PDFs.
# sentence-transformers>=2.2.0
# faiss-cpu>=1.7.4
# pypdf>=3.17.0
